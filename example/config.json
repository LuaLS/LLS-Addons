{
  "$schema": "https://raw.githubusercontent.com/carsakiller/LLS-Addons/main/schemas/addon.schema.json",
  "description": "An example configuration file for an addon",
  "filename_pattern": ["example_trigger%.lua"],
  "text_pattern": ["trigger_activation"],
  "settings": {
    "Lua.diagnostics.globals": ["myGlobalOne", "myOtherGlobal"],
    "Lua.diagnostics.disable": ["different-requires"],
    "Lua.workspace.preloadFileSize": 1000
  }
}
