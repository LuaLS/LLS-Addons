{
  "$schema": "../schemas/addon.schema.json",
  "description": "An example configuration file for an addon",
  "filename_pattern": ["example_trigger%.lua"],
  "text_pattern": ["trigger_activation"],
  "settings": {
    "diagnostics": {
      "globals": ["myGlobalOne", "myOtherGlobal"],
      "disable": ["different-requires"]
    },
    "workspace.preloadFileSize": 1000
  }
}
